import LoginForm from '../components/LoginForm';

/**
 * Login Page
 * Simple wrapper page for login form
 * Uses AuthLayout for consistent auth page styling
 */
const LoginPage = () => {
  return <LoginForm />;
};

export default LoginPage;

