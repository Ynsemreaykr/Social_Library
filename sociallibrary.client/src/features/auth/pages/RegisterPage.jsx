import RegisterForm from '../components/RegisterForm';

/**
 * Register Page
 * Simple wrapper page for register form
 * Uses AuthLayout for consistent auth page styling
 */
const RegisterPage = () => {
  return <RegisterForm />;
};

export default RegisterPage;

